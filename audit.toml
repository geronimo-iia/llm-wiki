# https://doc.rust-lang.org/cargo/commands/cargo-audit.html

[advisories]
ignore = [
    # boxfnonce unmaintained — transitive via agent-client-protocol 0.11
    "RUSTSEC-2019-0040",
    # lru unsound (use-after-free in LruCache::pop) — transitive via tantivy ^0.16.3;
    # tantivy 0.26.1 is latest and still pins lru ^0.16. Unblocks when tantivy bumps lru.
    "RUSTSEC-2026-0253",
    # atomic-polyfill unmaintained — transitive via petgraph-live -> postcard -> heapless ^0.7;
    # postcard pins heapless ^0.7.0; heapless 0.9 dropped it but postcard hasn't released a bump.
    "RUSTSEC-2023-0089",
]
